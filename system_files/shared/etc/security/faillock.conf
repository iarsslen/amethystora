# Account lockout after wrong passwords (authselect's with-faillock feature, 08-hardening.sh):
# 10 failures within 15 minutes lock the account for 10 minutes. root is never locked.
# Unlock early, as root: faillock --user <name> --reset
deny = 10
fail_interval = 900
unlock_time = 600
