# The SSH server is off by default. When turned on (`sudo systemctl enable --now sshd`), these apply.
# sshd keeps the first value it reads, so this file comes before Fedora's 50-redhat.conf.
PermitRootLogin no
PermitEmptyPasswords no
MaxAuthTries 3
LoginGraceTime 30
X11Forwarding no
