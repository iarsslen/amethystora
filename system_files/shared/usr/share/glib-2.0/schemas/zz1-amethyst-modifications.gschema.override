# Amethyst overrides, applied on top of zz0-bluefin-modifications from projectbluefin/common.
# GNOME Shell is not installed; these keys still style GTK/libadwaita applications.

[org.gnome.desktop.interface]
accent-color='purple'
