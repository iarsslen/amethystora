[Service]
# Label the greeter's home and cache with the rules set at build time (04-hyprland.sh). Directories created
# before those rules existed keep a label the greeter cannot write to, and it then fails with a black screen.
ExecStartPre=-/usr/sbin/restorecon -R /var/cache/dms-greeter /var/lib/greeter
