# Brave's RPM installs to /opt, which is /var/opt on a booted system and not part of the image.
# The image keeps it in /usr/lib/brave.com (04-packages.sh) so it updates with the system.
d /var/opt 0755 root root -
L+ /var/opt/brave.com - - - - /usr/lib/brave.com
