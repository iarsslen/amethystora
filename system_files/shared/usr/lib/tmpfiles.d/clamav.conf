# The image ships an empty /var: create ClamAV's signature database (filled by
# clamav-freshclam.service) and quarantine directories at boot.
d /var/lib/clamav 0755 clamupdate clamupdate -
d /var/spool/quarantine 0750 root root -
