# nordvpnd answers only root and members of this group (04-packages.sh). NordVPN's installer creates
# it with groupadd, which reaches only the image's own /etc/group, and a machine keeps its own
# /etc/group across updates. systemd-sysusers adds it there on the first boot after each update.
g nordvpn -
