#!/usr/bin/bash
# Starts the DMS login screen on Hyprland (greetd's default session, see /etc/greetd/config.toml).

# shellcheck source=system_files/shared/usr/lib/amethystora/graphics.sh
source /usr/lib/amethystora/graphics.sh

exec dms-greeter "$@"
